- const {nav: { items }} = require('../data.json');

nav.nav
    ul.nav__list
        each item in items
            - let {url, label} = item
            li.nav__item
                a.nav__link(href= `#${url}`)= label

